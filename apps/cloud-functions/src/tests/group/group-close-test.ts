// import * as admin from 'firebase-admin';
// import { groupCloseCall } from '../../group/group-close';
// import { _initializeFunctionsTest } from '../testing-util';

// test('Test [Cloud Functions]: groupCloseCall', () => {
//   const fft = _initializeFunctionsTest();

//   admin.initializeApp();
//   //admin.functions().useFunctionsEmulator('http://localhost:5000');
//   //const wrappedGroupCloseCall: WrappedV2Function<CloudEvent<unknown>>;

//   //beforeEach(() => {
//   const wrappedGroupCloseCall = fft.wrapV2(groupCloseCall);
//   //});

//   // it('should work', () => {
//   const res = await wrappedGroupCloseCall({ data: '1234', auth: { uid: 'userId' } });

//   console.log('res', res);
//   //res.

//   //  const wrappedFunction = fft.wrap<
//   //    CallableV2Request<typeof groupCloseCall>
//   // >(groupCloseCall);
//   //});
// });
