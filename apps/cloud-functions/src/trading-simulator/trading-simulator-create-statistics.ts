import { TradingSimulator } from '@mm/api-types';

export const tradingSimulatorCreateStatistics = async (simulator: TradingSimulator) => {
  // todo  - statistics into TradingSimulatorAggregations
};
