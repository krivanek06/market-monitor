/**
 * loads trading simulators and :
 * - increments round
 * - update each participant's portfolio
 * - recalculate user ranking
 */
export const tradingSimulatorVerifyCurrentRound = async () => {
  // load exactly which needs to be incremented
};
