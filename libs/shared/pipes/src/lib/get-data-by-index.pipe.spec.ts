import { GetDataByIndexPipe } from './get-data-by-index.pipe';

describe('GetDataByIndexPipe', () => {
  it('create an instance', () => {
    const pipe = new GetDataByIndexPipe();
    expect(pipe).toBeTruthy();
  });
});
