# general-util

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build general-util` to build the library.

## Running unit tests

Run `nx test general-util` to execute the unit tests via [Jest](https://jestjs.io).
