import { ObjectValueByKeyPipe } from './object-value-by-key.pipe';

describe('ObjectValueByKeyPipe', () => {
  it('create an instance', () => {
    const pipe = new ObjectValueByKeyPipe();
    expect(pipe).toBeTruthy();
  });
});
