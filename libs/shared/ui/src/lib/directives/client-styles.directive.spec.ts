import { ClientStylesDirective } from './client-styles.directive';

describe('ClientStylesDirective', () => {
  it('should create an instance', () => {
    const directive = new ClientStylesDirective();
    expect(directive).toBeTruthy();
  });
});
