import { PositionColoringDirective } from './position-coloring.directive';

describe('PositionColoringDirective', () => {
  it('should create an instance', () => {
    const directive = new PositionColoringDirective();
    expect(directive).toBeTruthy();
  });
});
