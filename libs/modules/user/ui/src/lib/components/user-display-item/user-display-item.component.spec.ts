import { MockBuilder } from 'ng-mocks';
import { UserDisplayItemComponent } from './user-display-item.component';

describe('UserDisplayItemComponent', () => {
  beforeEach(() => {
    return MockBuilder(UserDisplayItemComponent);
  });

  it('should create', () => {
    // expect(component).toBeTruthy();
  });
});
