import { GroupUserRoleDirective } from './group-user-role.directive';

describe('GroupPersonPositionDirective', () => {
  it('should create an instance', () => {
    const directive = new GroupUserRoleDirective();
    expect(directive).toBeTruthy();
  });
});
