import { MockBuilder } from 'ng-mocks';
import { FormRegisterComponent } from './form-register.component';

describe('FormRegisterComponent', () => {
  beforeEach(() => {
    return MockBuilder(FormRegisterComponent);
  });

  it('should create', () => {
    // expect(component).toBeTruthy();
  });
});
