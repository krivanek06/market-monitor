import { MockBuilder } from 'ng-mocks';
import { FormLoginComponent } from './form-login.component';

describe('FormLoginComponent', () => {
  beforeEach(() => {
    return MockBuilder(FormLoginComponent);
  });

  it('should create', () => {
    // expect(component).toBeTruthy();
  });
});
