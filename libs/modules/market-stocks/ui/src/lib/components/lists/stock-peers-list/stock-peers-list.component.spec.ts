import { MockBuilder } from 'ng-mocks';
import { StockPeersListComponent } from './stock-peers-list.component';

describe('StockPeersListComponent', () => {
  beforeEach(() => {
    return MockBuilder(StockPeersListComponent);
  });

  it('should create', () => {
    // expect(component).toBeTruthy();
  });
});
