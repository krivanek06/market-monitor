import { GetStocksSummaryPipe } from './get-stocks-summary.pipe';

describe('GetStocksSummaryPipe', () => {
  it('create an instance', () => {
    const pipe = new GetStocksSummaryPipe();
    expect(pipe).toBeTruthy();
  });
});
