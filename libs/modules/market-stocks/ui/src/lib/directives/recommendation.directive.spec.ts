import { RecommendationDirective } from './recommendation.directive';

describe('RecommendationDirective', () => {
  it('should create an instance', () => {
    const directive = new RecommendationDirective();
    expect(directive).toBeTruthy();
  });
});
